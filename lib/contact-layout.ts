/** Top inset shared by left column + form card (eyebrow baseline). */
export const CONTACT_PANEL_TOP = "pt-5 sm:pt-8 lg:pt-9"

/** Full form-card inset (horizontal + top). */
export const CONTACT_PANEL_INSET = `px-5 sm:px-8 lg:px-9 ${CONTACT_PANEL_TOP}`
